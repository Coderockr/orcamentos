{% extends "layout.twig" %}
{% block content %}
    <div id="main" class="container">
        <div class="row">
            <div class="col-md-9">
                <h3>Projetos</h4>
            </div>
            <div class="col-md-3">
                <a href="/project/edit" class="btn btn-primary" title="Cadastre um novo projeto">Novo Projeto</a>
            </div>
        </div>

        <div class="row cards">
            {% for project in projectCollection %}
                <div class="col-md-4">
                    <a href="/project/edit/{{ project.id }}" class="thumbnail" title="Visualizar projetos e orçamentos">
                        <span class="caption">
                            <h4>{{ project.name }}</h4>
                            <p>
                                {{ project.client.name }}<br />
                                {{ project.tags }}
                            </p>
                        </span>
                    </a>
                    <a href="#" title="Editar projeto" class="edit"><i class="glyphicon glyphicon-pencil"></i>editar</a>
                </div>
            {% endfor %}
        </div>

        <div class="row">
            <div class="col-md-12">
                <ul class="pagination btn-primary">
                    <li class="active"><a href="#">&lt;</a></li>
                    <li><a href="#">1</a></li>
                    <li class="active"><a href="#">2</a></li>
                    <li><a href="#">3</a></li>
                    <li><a href="#">4</a></li>
                    <li class="active"><a href="#">&gt;</a></li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}